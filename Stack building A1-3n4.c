#include <stdio.h>

// 3. int str_compare(char *str1, char *str2) - if str1 is less than str2 the function should return -1 
// if str1 is greater than str2 it should return 1 and if both are equal then return 0

int str_compare(char *str1, char *str2)
{
  char *str1, *str2; int i, j=0, h=0;
  for (i = 0; *Str1[i]!= '\0'; ++i);
  { 
  str1(i)=j++
  }
  for (i = 0; *Str2[i]!= '\0'; ++i);
  { 
   str2(i)=h++
  }
 {
    if (j<h);
    return -1;
    else if(j>h);
    return 1;
 }
return 0;
}

// 4. int str_find_char(char *str, char *ch) - returns the position where ch is in str and if not present returns -1

int str_find_char(char *str, char *ch)
{
  char *str1, char *ch;
  for (int i = 0; *Str1[i]!= '\0'; ++i);
  { 
  str2(i)==ch  
  return i;
  }
  return -1;
}

void main()
{
    char str1[100],str2[100]; int i;
    printf("Enter a String:\n");
    scanf("%s", str1);
    int str_compare(char *str1, char *str2)    
    int str_find_char(char *str, char *ch)
    
return 0;
}
